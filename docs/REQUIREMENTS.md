# Requirements — xnat_seg_wrapup

The shared post-processing step for every wrapped segmentation model in the XNAT
model catalog (see the epic in `xnatworks/business/ai-foundation-models/EPIC-MODEL-CATALOG.md`).
Runs as a Container Service wrapup command; upstream model images stay untouched.

| # | Requirement | Status | Notes |
|---|---|---|---|
| R1 | Run as a `docker-wrapup` command: no mounts/inputs/outputs declared, reads the parent's output at `/input`, writes the upload set to `/output` | **Done** | Live on demo02 as command 674, 2026-09-02. No `ENTRYPOINT` in the image: CS runs the command-line without overriding it |
| R2 | Per-structure volumes (mL from the image's own spacing) as `volumes.json` and `volumes.csv` | **Done** | 117-structure TotalSegmentator output measured on the CPTAC scan |
| R3 | Self-contained HTML volumetrics report, light and dark, no external assets, research-only footer | **Done** | Rendered in headless Chromium (light and dark) 2026-09-02 from the live TotalSegmentator output: 78 rows, chips, bars, footer all correct |
| R4 | Viewer label files: ITK-SNAP `labels.txt` and 3D Slicer `labels.ctbl`, stable golden-angle colours mirrored as chips in the report | **Done** | |
| R5 | Label names from the model's own table: `labels.json`, nnU-Net `dataset.json` (v1/v2), MONAI `metadata.json`, ITK-SNAP, CSV; auto-discovered under `/input` | **Done** | TotalSegmentator card writes `labels.json` from its `class_map` inside the model container |
| R6 | Merge one-file-per-structure binary masks into one label map (TotalSegmentator default layout), reproducible label assignment, overlaps logged | **Done** | Auto-detected; `--merge no` to disable |
| R7 | DICOM SEG from the mask and the source series when the parent copied it to `/output/.source_dicom`; masks on a different grid refused, never resampled | **Done** | BINARY type; a 117-segment × 203-frame SEG is ~116 MB. LABELMAP encoding would be far smaller: see R9 |
| R13 | DICOM SEG on de-identified series: Type 2 patient/study attributes that an anonymiser stripped (TCIA drops `PatientBirthDate` etc.) are added empty before highdicom copies them, instead of failing the SEG | **Done** (0.2.1) | Found on the first MuscleMap run (STS_PETMR); two tests in `tests/test_dicomseg.py` |
| R14 | One colour per structure everywhere: DICOM SEG segments carry `RecommendedDisplayCIELabValue`; a BIDS `.tsv` lookup beside each map for the XNAT workbench; an 8-bit renumbered companion (+ `.tsv`) when values exceed 255 | **Done** (0.2.2) | Six tests across `test_labels.py`, `test_volumes.py`, `test_cli.py`, `test_dicomseg.py` |
| R8 | Failure policy: no mask → fail; report/label/SEG failure → logged, masks still delivered | **Done** | Tested |
| R9 | LABELMAP segmentation type (DICOM 2024) to shrink multi-structure SEGs; keep BINARY where the viewer cannot read LABELMAP | Not started | **Rendering risk retired 2026-09-03**: OHIF renders a 95-segment BINARY SEG (FastSurfer, 23.0 MB) correctly, overlay and all 95 named labels, so BINARY is not a viewer problem even at the catalog's largest segment count. Whether OHIF reads LABELMAP is still unverified. R9 is therefore a storage argument — 23 MB a brain, 116 MB for a 117-segment TotalSegmentator SEG — not a correctness one |
| R10 | Register the SEG as an ROI collection so OHIF lists it (`/xapi/roi/...`) | **Done** | Context arrives as environment from the parent's derived inputs (`SEG_PROJECT`, `SEG_SESSION_ID`, `SEG_SCAN_ID`) plus CS's `XNAT_*`; failure logged, not fatal |
| R15 | Drop `segmentation.seg.dcm` from the delivered resource once the ROI collection has been registered with the same bytes; keep it whenever no collection was created (registration skipped, no XNAT context, or failed), and `--keep-seg-file` to override | **Done** (0.2.4) | The collection is the SEG's home — OHIF's segmentation badge is driven by the collection, not by the file on the scan. Keeping both duplicated ~20 MB a run (measured on a 512x512x50 MR station). Four tests in `tests/test_register.py`, including the three keep-it paths |
| R16 | Single-segment SEGs must register as ROI collections: `SegmentIdentificationSequence` is moved out of `SharedFunctionalGroupsSequence` into every per-frame group | **Done** (0.2.5) | highdicom hoists a functional group into the shared groups when its value is constant across frames, which for a one-label mask it always is. Legal DICOM, but XNAT's ROI plugin reads the macro per-frame only and returns HTTP 500 `SegmentIdentification missing`. Found on the first DeepWMH run (UPENN-GBM-00001, 2026-09-04); it silently affected every single-lesion model while multi-structure models were unaffected. Two tests in `tests/test_dicomseg.py`, including the multi-segment control |
| R11 | RTStruct output option | Not started | |
| R12 | Merge one-multilabel-map-per-model outputs (MOOSE) with label offsets; per-mask sidecar label files; MOOSE `organ_indices.json` format | **Done** (0.2.0) | Eight tests in `tests/test_multilabel_merge.py` |
| R17 | When `XNW_CONTRACT` is set, publish one `analysis:sessionAnalysisData` record per run (type/status/QC/provenance as fields, the run's output as resources, never a measurement field); no contract, no record; failures recorded in `wrapup.json`, never fatal | done 0.3.0 | `tests/test_publish.py` |
| R18 | **Roles are views onto the output tree, not a partition of it** (plan D20). The record has four resources: `DERIVED` = the tool's complete output tree at the resource root, byte-for-byte and path-for-path, never a wrapup file; `REPORT`, `PROVENANCE`, `LOGS` = only what the wrapup generated (`status.json`/`prereq.json` the card wrote into the tool's `/output` are lifted into `PROVENANCE`). Every other role a card names (`METRICS`, …) is a view: DERIVED paths recorded in `wrapup.json` and `results_json`, never a copy. A card's globs for a fixed resource are ignored and logged. record-fetch resolves `role=<view>` through the mapping; pre-0.6.0 records keep serving their resource | **Done** (0.6.0) | `docs/ROLES-AS-VIEWS.md`; `tests/test_publish.py` (tree at the root, tool HTML report stays with its figures, legacy `raw/` glob rebased, stray file left off), `tests/test_proc.py` (BIDS dataset end to end, `status.json`/`prereq.json` never in DERIVED, report links), `tests/test_prereq.py` (view resolution, legacy fallback, unknown view). Live defect it fixes: E25614 (mriqc, three resources) and E25617 (fmriprep, `raw/sub-H025.html` alone in REPORT, confounds carved out) |
| R19 | `--pointer-only` leaves nothing but `wrapup.json` for the output handler: files the record could not take because they were empty are removed as well, so a skipped file is uploaded nowhere | **Done** (0.6.0) | `tests/test_proc.py::test_pointer_only_removes_the_empty_files_the_record_could_not_take`. Live defect: `XNW_BIDS` (2 files) and `XNW_FMRIPREP` (3 files) on demo02 after 0.5.0 |
| R20 | `DERIVED` keeps the dataset's dotfiles (`.bidsignore`, `.heudiconv/`, dot-prefixed state files at any depth) at their paths; only the entries the wrapup contract reserves by name at the tree root (`.source_dicom`, the DICOM copy XNAT already holds) and the explicit `skip` paths a wrapup passes are left out. No dot-prefix rule inside the tool's tree | **Done** (0.6.1) | `docs/ROLES-AS-VIEWS.md` (0.6.1 section); `tests/test_publish.py` (tree with `.bidsignore`, `.heudiconv/`, a subdirectory dotfile, `.source_dicom` at both roots; seg-wrapup `raw/` shape; CLI end to end), `tests/test_proc.py` (`copy_raw_output`, proc-wrapup end to end). Live defect: 0.6.0 dropped QSIRECON's `.bidsignore` (demo02 XNAT_E09349 reference). XNAT accepts the dot-prefixed name: the in-body PUT path (`SecureResource` → `FileList` → `CatalogUtils.storeCatalogEntry`) has no leading-dot check, and a live listing on demo02 already shows `DERIVED/.../files/.bidsignore` with a catalog id; XNAT < 1.8.3 dropped root-level hidden files on catalog *refresh* (XNAT-6820), see the trap in `docs/ROLES-AS-VIEWS.md` |
| R21 | A subject-scoped run (`PROC_SUBJECT_ID`, no session id) publishes an `analysis:subjectAnalysisData` record under the subject, same fields as a session record minus `scans`, files as experiment resources, `inputs_json` naming scope, subject and the sessions spanned | **Done** (0.6.2) | `docs/SUBJECT-SCOPE.md`; `tests/test_publish.py` (subject XML, URLs, rollback), `tests/test_register.py` (context, labels), `tests/test_proc.py` (end to end). Live: demo02 subject 292 (ds002799), mriqc-subject then fmriprep-subject |
| R22 | At subject scope a session-scoped prerequisite holds on every session of the subject (per-session layout and manifest); `scope=subject` selects a subject record; a session run falls back to the subject's record when none of its own fits; a failed gate is recorded on the subject | **Done** (0.6.2) | `docs/SUBJECT-SCOPE.md`; `tests/test_prereq.py` (scope rules) |
| R24 | At subject scope a session-scoped record prerequisite is satisfied by the subject's own record first (one dataset spanning every session, laid out at `prereq/<name>/`), else on every session; the unmet message names both | **Done** (0.6.3) | `docs/SUBJECT-SCOPE.md`; `tests/test_prereq.py::test_a_subject_run_takes_the_subjects_own_record_before_per_session_records` |
| R25 | The record-fetch image label advertises `record-fetch` and `record-fetch-subject`, identical to `commands/*.json`; the proc-wrapup label matches its JSON | **Done** (0.6.3) | `tests/test_cli.py` (label tests for all three images) |
| R26 | Every record carries a copy of its card under `PROVENANCE/card/`: `metadata.json` = the command's `command-metadata.card` read back from the Container Service (the card's metadata verbatim, plus digest and README/LICENSE links), and `card.json` = what only the run knew (command and wrapper ids, wrapup and version, scope, when); seg-wrapup, proc-wrapup and the record-fetch failure record alike; no block or unreadable command → `card.json` with the reason; never fatal (plan D27) | **Done** (0.6.3; the adopt tool writes `command-metadata.card`, container-workshop) | `tests/test_card.py`; `tests/test_proc.py` (end to end, `PROVENANCE/files/card/...`), `tests/test_prereq.py` (failure record) |
| R23 | `results_json` never exceeds the schema's 65,536-character cap: per-role counts, view lists reduced to counts with `truncated` when they would overrun | **Done** (0.6.2) | `tests/test_publish.py` (3,000-file tree). Live defect: fmriprep full run and hippunfold refused on demo02, 2026-09-09 |
